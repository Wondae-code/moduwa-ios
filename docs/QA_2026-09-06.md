# 앱 QA 리스트 (2026-09-06)

기획·디자인 쪽에서 온 QA 지적 19건. **새 작업을 시작할 때 이 파일을 먼저 읽는다.**
항목을 끝낼 때마다 이 파일의 체크박스와 "메모"를 갱신한다 — 다음 세션이 원인 조사를
처음부터 다시 하지 않게 하는 것이 이 파일의 목적이다.

## 원문 (받은 그대로)

```
1. 음성 tts : 선택시 파형 아이콘으로 교체. 읽는 부분 체크하는 연두색 띠 삭제.
   선택시 딥그린 원 크기 크게. 중복 데이터 삭제
2. 마이페이지 개선 : 3 depth 구조에서 2 depth로 개선
3. 홈 화면에서 피드 정렬 변경 ui 교체
4. 홈 화면에서 최신 피드 안 가져오는것 수정
5. Apple 로그인 버튼 수정 : 다른 컴포넌트와 맞추기
6. 피드에서 이미지 없는 곳은 피그마 따라가기 : card default. 흰색 아이콘 사용
7. 장소 상세 아이콘 투명도 제거.
8. 설명 더 보기 버튼 모션 제거
9. 장소 상세 상단 메뉴 제거
10. 글 쓰기 페이지 ui 변경
11. 홈 화면 피드 카드 수정
12. 장소 리뷰 쓸 때 pill에 아이콘 없애기
13. 플랜에서 여행 수정가능하게 하기
14. 플랜 상세 부분 피그마랑 비교해서 수정하기
15. 글자 크기 수정 시 메인으로 돌아가는 버그
16. 여행 상세에서 지도 아이콘 피그마에 있는것으로 대체
17. 프로필에서 연필 대신 우상단에 프로필 편집 추가
18. 프로필에서 무장애 정보 편집 위에 무장애 정보 표시
19. 이미 만들어진 플랜도 Ai로 대체 할 수 있게 (다음에)
```

19번은 **요청자가 직접 미뤘다**(다음에). 실제 대상은 18건.

## 진행 상태

**1묶음(버그)·2묶음(장소 상세) 끝. 3묶음부터 남음.** 2026-09-06 기준.

| # | 항목 | 묶음 | 상태 |
|---|------|------|------|
| 15 | 글자 크기 → 메인 복귀 버그 | 1 버그 | ☑ **완료** · 시뮬레이터 확인 |
| 4 | 최신 피드 안 가져옴 | 1 버그 | ◐ **앱 몫 완료** · 서버 답 오면 한 줄(아래 참고) |
| 1 | 음성 TTS 네 가지 | 2 장소상세 | ☑ **완료** · 소리·파형은 실기기 |
| 7 | 아이콘 투명도 제거 | 2 장소상세 | ☑ **완료** |
| 8 | 설명 더 보기 모션 제거 | 2 장소상세 | ☑ **완료** · 눌림 흐려짐만 남음 |
| 9 | 상단 메뉴 제거 | 2 장소상세 | ☑ **완료** · 시안에 ☰ 가 없음을 확인 |
| + | 헤더에 읽어주기 버튼(시안 249:712) | 2 장소상세 | ☑ **완료** (추가 요청) |
| + | 저장됨이면 북마크 채우기 | 2 장소상세 | ☑ **완료** (추가 요청) · 실기기 확인 필요 |
| 3 | 피드 정렬 변경 UI 교체 | 3 홈 | ☑ **완료**(09-07) · 직접 만들고 시스템 모양에 맞춤 |
| 6 | 이미지 없는 카드 default | 3 홈 | ☑ **완료**(09-07) |
| 11 | 피드 카드 수정 | 3 홈 | ☐ 피그마 필요 |
| 2 | 마이페이지 3→2 depth | 4 프로필 | ☐ 질문 대기 |
| 17 | 프로필 편집을 우상단으로 | 4 프로필 | ☐ |
| 18 | 무장애 정보 표시 | 4 프로필 | ☐ |
| 5 | Apple 로그인 버튼 규격 | 4 프로필 | ☐ 질문 대기 |
| 13 | 플랜에서 여행 수정 | 5 플랜 | ☐ 질문 대기(범위) |
| 14 | 플랜 상세 피그마 대조 | 5 플랜 | ☐ 피그마 필요 |
| 16 | 지도 아이콘 교체 | 5 플랜 | ☐ 피그마 필요 |
| 10 | 글쓰기 페이지 UI | 6 글쓰기 | ☐ 피그마 필요 |
| 12 | 리뷰 pill 아이콘 제거 | 6 글쓰기 | ☐ |
| 19 | 기존 플랜도 AI로 | — | 보류(요청자가 미룸) |

### 하다가 발견해서 아직 안 고친 것 (QA 목록 밖)

| 무엇 | 어디 | 왜 미뤘나 |
|---|---|---|
| "설명 더보기" 버튼이 **접을 것이 없을 때도 뜬다** | 장소 상세 `overviewSection` | `JustifiedText` 가 **잘렸는지**를 알려 줘야 한다(6줄 제한 높이 vs 전체 높이 비교). #8 과 별건 |
| 헤더 버튼들이 44pt 터치 영역에 못 미친다 | 장소 상세 헤더(뒤로 25 · 지도 26) | 넓히면 12pt 간격의 이웃과 겹친다. 헤더 전체를 다시 재는 일이라 따로 |

### 남이 해 줘야 끝나는 것

| 무엇 | 누구 | 상태 |
|---|---|---|
| `/v1/reviews?sort=recommended` 에 `myVisitorTags` | 서버 | 요청서 냈다 — `docs/BACKEND_REQUEST_access-first-recommend.md`. 오면 **앱은 한 줄** |
| `GET /v1/reviews` 항목 태그에 `kind` 실어 주기 | 서버 | 같은 요청서에 곁들였다. **막혀 있지는 않다**(코드로 우회 중) |
| 파형 아이콘 에셋 | 디자이너 | 시안에 없어 애플 기본 `waveform` 으로 갔다. 시안 에셋을 주면 갈아 끼운다 |
| 정렬 목록의 "팔로잉" 항목 | 기획 | 팔로우 API 가 서버에 **아예 없다**. 넣지 않기로 했다(09-07) |
| `GET /v1/places/:id/related` 에 카테고리 | 서버 | 없어서 "함께 가볼만한 곳" 의 사진 없는 카드에 아이콘을 못 그린다 |

### 실기기에서만 확인되는 것

시뮬레이터로는 못 본 것들. TestFlight 빌드를 올릴 때 함께 본다.

- **낭독 소리와 파형 움직임** — 시뮬레이터에 오디오 출력 장치가 없어 낭독이 즉시 끝난다
- **저장됨 북마크 채워짐** — 저장은 로그인이 필요하다(`isSaved` 를 잠시 고정해 모양만 봤다)
- **후기를 쓰면 홈 피드가 갱신되는지** — 후기 작성도 로그인이 필요하다

## 묶음 나눔 — 화면 단위

한 묶음 = 커밋 묶음 = 빌드 한 번 = QA 한 번. 화면이 같으면 같은 파일을 만지므로 함께 한다.

### 1묶음 · 버그 둘 (먼저)

UI를 건드리기 전에 잡는다. 화면이 바뀐 뒤에 버그를 재현하면 무엇 때문인지 헷갈린다.

**#15 글자 크기 → 메인 복귀 — 원인 확인됨**

`moduwa/Services/AccessibilitySettings.swift` 의 `applyTextScale(_:)` 가

```swift
if let size = scale.dynamicTypeSize { dynamicTypeSize(size) } else { self }
```

이다. `@ViewBuilder` 의 if/else 는 `_ConditionalContent` 를 만들어 **분기가 바뀌는 순간 뷰
정체성이 바뀐다**. 이 모디파이어가 `RootView.body` 의 `tabs` 전체에 걸려 있어서(`RootView.swift:69`),
**시스템 ↔ 크게**로 넘어갈 때 SwiftUI 가 트리를 통째로 새로 만들고 각 탭 `NavigationStack` 의
경로가 날아간다 → 메인으로 돌아간다.

**→ 고쳤다(2026-09-06).** `transformEnvironment(\.dynamicTypeSize)` 로 바꿔 **어떤 단계에서도
모디파이어가 늘 하나**만 걸리게 했다. `.system` 이면 물려받은 값을 건드리지 않아 결과는 예전과
같고, 분기가 없어져 정체성도 안 바뀐다. 이유는 그 함수 주석에 남겼다.

시뮬레이터 확인(iPhone 17 Pro, iOS 26.4) — 홈 → 설정 → 접근성까지 두 단 들어간 상태에서
**시스템 → 크게 → 아주 크게 → 시스템**을 모두 옮겼고, 매번 접근성 화면에 그대로 남았다.
글자도 단계마다 실제로 커졌다(모디파이어가 헛돌지 않는다는 확인).

같은 함수를 쓰는 `AccessibilitySettingsView` 의 미리보기 카드도 함께 고쳐졌다.

**#4 홈 최신 피드 — 고쳤다(2026-09-06). 원인이 셋이었다.**

라이브 서버에 직접 물어 확인했다(`GET /v1/reviews`):

| 정렬 | 첫 5건의 작성일 |
|------|----------------|
| `recommended`(앱의 옛 기본값) | 07-11, 07-09, 07-07, 07-12, 06-30 — **전부 두 달 전** |
| `latest` | **09-06**, 08-22, 08-09, 08-08, 08-08 |

그날(09-06) 올라온 후기 "허스크밀"은 반응이 0 이라 추천순 12건 중 뒤로 밀려 **첫 페이지에
아예 없었다.** 앱을 처음 켠 사람이 두 달 전 글을 봤다. 원인 셋을 모두 고쳤다:

1. ~~기본 정렬을 `.recommended` → `.latest`~~ → **되돌렸다(추천 우선 유지, 기획 결정).**
   따라서 **최신 글이 위로 오지 않는 것은 지금도 그대로다.** 새 글을 위로 올려야 한다면
   정렬 기준 자체를 손대야 한다(아래 "추천 기준" 참고) — 서버 일이다.
2. **홈에 당겨서 새로 고침을 붙였다**(`HomeViewModel.refresh`). 홈에는 다시 받을 길이
   아예 없었다 — `.task` 는 돌아올 때 다시 돌지 않는다. 저장·내 글·차단 목록에는 이미
   있던 동작이다. 실패한 목록은 지우지 않는다(당긴 순간 네트워크가 흔들리면 보던 것이 사라진다).
3. **후기를 쓰면 홈에 알린다**(`PostInteractionSignal.reviewWritten()`). 게시글은
   `WriteFloatingButton.onPosted` 로 이미 알렸지만 **후기는 장소 상세에서 쓰기 때문에 홈이
   그 일을 몰랐다** — 앱을 다시 켜야 보였다. 후기를 쓰는 두 화면(장소 상세·장소 후기 목록)이
   신호를 올리고 홈이 `.task(id:)` 로 지켜본다.

시뮬레이터 확인(iPhone 17 Pro): 정렬 칩이 "최신"으로 뜨고 **그날 쓴 후기가 피드 첫 줄**에
올라왔다(고치기 전에는 목록에 없었다). 당겨서 새로 고침은 "맞춤 추천 더보기"로 2페이지까지
늘린 뒤 당기면 목록이 1페이지로 되돌아가는 것으로 확인했다.

⚠️ 3번(후기 신호)은 **로그인이 필요해 시뮬레이터에서 끝까지 못 봤다** — 실기기 확인 목록.

**추천 기준(서버 `REVIEW_ORDERS`, `src/server/app.ts:934`)**

| sort | ORDER BY |
|------|----------|
| `recommended` (기본) | `(like_count + comment_count) desc, created_at desc` |
| `latest` | `created_at desc` |
| `likes` (장소 후기 화면 전용) | `like_count desc, created_at desc` |

좋아요와 댓글을 **1:1 로 더한 값**만 본다. 최신성은 그 합이 같을 때의 두 번째 키일 뿐이라
반응이 하나라도 붙은 오래된 글이 갓 올라온 글보다 늘 위에 선다. 시간 감쇠(오래된 반응의
가중치를 낮추는 것)는 없다.

`/v1/posts` 에는 **정렬 파라미터가 아예 없다**(`created_at desc` 고정) — 그래서 앱이
`HomeViewModel.feedItems` 에서 게시글에 같은 규칙을 다시 적용해 섞는다.

### 추천 기준을 접근성 우선으로 바꿨다(2026-09-06)

기본 정렬은 추천으로 두고, **추천의 뜻**을 고쳤다. 앱의 `.recommended` 는 이제

**① 내 무장애 축과 겹치는 수 → ② 반응 수 → ③ 최신**

순으로 세운다(`HomeViewModel.feedItems`). 접근성이 주제인 앱에서 "추천"이 반응 수만
뜻하면 나와 상관없는 조건의 글이 위에 선다 — 휠체어로 다니는 사람에게 시각장애 후기가
인기순으로 먼저 오는 식이다. **서버 `REVIEW_ORDERS` 와 일부러 달라진 자리다.**

"이 글이 어떤 축인가"는 두 종류가 다른 자리에 담고 있어 `HomeFeedItem.accessFeatures` 가
한 뜻으로 모은다 — 후기는 **방문 조건 태그**(`visit_*`), 게시글은 `accessFeatures`.
겹침을 여부(0/1)가 아니라 **개수**로 세므로 축을 둘 고른 사람에게 둘 다 맞는 글이 위로 온다.
축을 고르지 않은 사람(해당없음·비로그인)은 겹침이 늘 0 이라 예전과 똑같이 동작한다.

**함정 하나(실측으로 걸렸다)** — 후기 태그를 `kind == .visitor` 로 걸러선 안 된다.
`GET /v1/reviews` 항목의 태그에는 **`kind` 가 실리지 않는다**(`reviewSelect`, app.ts:891 —
`code`·`label`·`shortLabel`·`icon` 뿐). `kind` 는 정의 목록 `GET /v1/review-tags` 에만 있다.
그래서 `ReviewTag.kind` 는 기본값 `.place` 로 디코드되고, `kind` 로 걸렀다면 방문 조건 태그가
**하나도** 걸리지 않았다. 코드로 되찾으면(`AccessibilityFeature(visitorTagCode:)`)
`visit_*` 만 정확히 맞으므로 가를 필요가 없다.

**시뮬레이터 확인** — 내 축을 "시각 지원"으로 두고 정렬을 추천으로 둔 상태에서,
반응 0 인 게시글("오뎅탕", wheelchair+visual)이 반응 6 인 글("안녕")을 **제치고 1위**로 왔다.

⚠️ **아직 반쪽이다 — 받아 온 것 안에서만 다시 세운다.**
서버는 여전히 반응 수 순으로 페이지를 자른다. 게시글은 10건을 한 번에 받아(전체가 10여 건)
사실상 전부 대상이지만, **후기는 첫 페이지 5건뿐**이다. 실제로 시각 축에 맞는 후기
"허스크밀"(`visit_visual`, 반응 0)은 반응 수로 12건 중 6위라 **2페이지에 있어 홈에 못 온다.**
막으려면 셋 중 하나가 필요하다:

**→ 2번(서버)으로 간다. 요청서를 냈다: `docs/BACKEND_REQUEST_access-first-recommend.md`**
(2026-09-06). `GET /v1/reviews?sort=recommended` 에 `myVisitorTags` 를 받아 정렬 1순위 키로
쓰는 요청이다. **앱 쪽은 파라미터 이름이 확정되면 한 줄**이라 그때 붙인다 — 그동안 앱의
페이지 안 재정렬은 그대로 동작한다.

검토했다가 접은 것:
1. 앱에서 `visitorTag` 필터로 한 번 더 받아 앞에 붙이기 — 축 하나당 요청 하나가 늘고,
   두 목록의 페이지네이션을 앱이 또 관리해야 한다.
3. 그대로 두기 — 후기가 쌓이면 확률은 올라가지만 지금 데이터로는 효과가 0 이다.

⚠️ 1·2 모두 **무장애 축을 서버로 보낸다.** 참고로 지금도 홈은 추천 장소를 받을 때
`access=<groups>` 를 쿼리로 보내고 있다(`APIFeedService.fetchRecommendedPlaces`) —
새로운 종류의 전송은 아니지만, 쿼리스트링은 서버 접속 로그에 남는다는 점은 별개로 짚어 둘 만하다.

### 2묶음 · 장소 상세 (1, 7, 8, 9)

전부 `moduwa/Features/PlaceDetail/PlaceDetailView.swift` 한 파일이다.

- **#9 상단 메뉴 제거 — 완료.** 헤더의 햄버거 버튼 + "메뉴는 아직 준비 중이에요" 팝오버 +
  `showsMenuNotice` 상태를 지웠다. **심사 지적 "준비 중 버튼" 하나가 같이 없어졌다**
  (`docs/APPSTORE_REVIEW_2026-09-01.md`). 지도 버튼은 남겼다 — 카카오맵으로 나가는 **유일한
  동선**이다(시안에서 지도 아래 "카카오맵에서 보기" 링크가 hidden 처리됐다).
- **#7 아이콘 투명도 — 완료.** `.opacity(isOn ? 1 : 0.65)` 를 지웠다.
  `isOn` 이 켜지는 곳은 **저장하기 하나뿐**(저장됨 상태)이고 나머지 셋(일정추가·후기쓰기·공유)은
  켜질 일이 없어서 **늘 65% 로 흐린 채** 서 있었다 — 비활성 버튼처럼 읽힌다.
  지워도 뜻을 잃지 않는다: 저장됨 상태는 색이 아니라 **글자**가 말한다("저장하기 → 저장됨" +
  굵게). `isOn` 은 그 굵기 때문에 남기고, **죽은 `systemIcon`**(읽어주기가 이 줄에 있을 때
  쓰던 SF 심볼 경로)만 함께 정리했다.
- **#1 TTS — 네 가지 모두 완료(2026-09-06).**
  - **파형 아이콘**: 읽는 중에는 애플 기본 `waveform` 으로 바뀐다. 시안에 파형이 없어서
    (피그마 `Asset_Icon`·`tts` 노드·디자인 시스템 전부 0건) 기본 심볼로 갔고, 그 대신
    `symbolEffect(.variableColor.iterative, options: .repeating)` 로 **소리 나는 동안 막대가
    실제로 움직인다.** 꺼진 상태는 시안대로 헤드폰(`detail_tts`) 그대로.
  - **연두색 띠 삭제**: `speakingLine(_:)` 확장과 호출 3곳, `isSpeaking(_:line:)` 을 지웠다.
    딸려 죽은 것도 함께 정리했다 — `SpeechReader` 의 `segment`·`range` 와
    `willSpeakRangeOfSpeechString` 델리게이트, `utterances` 의 조각 번호. 강조에만 쓰이던 것들이다.
  - **딥그린 원 크게**: 26 → **34pt**(추가정보 뱃지와 같은 크기). 자리는 꺼진 상태에도 34pt 로
    잡아 둔다 — 켤 때 제목이 밀리지 않게.
  - **중복 데이터**: 추가정보의 같은 문장 반복. 원인은 고령자 그룹이 지체장애 그룹과 원문을
    공유하는 것이었다(위 질문 1 참고).

  - **헤더에도 읽어주기 버튼**(2026-09-06 추가 요청). 시안 헤더의 `menu`(249:711)는
    **`tts` + `map` 두 개**이고 헤드폰이 지도 **왼쪽**이다 — 그 자리를 놓치고 있었다.
    (햄버거는 시안에 아예 없다 → #9 가 맞았다.) **이 버튼만 화면 전체**를 보이는 순서대로
    읽는다(이름 → 주소 → 설명 → 기본정보 → 추가정보). 섹션 버튼은 그 섹션만 읽는다.
    ⚠️ 이전에 "섹션 단위로만" 이라고 정한 것과 어긋나 보일 수 있다 — 좁혀야 하면 알려 달라.

    원 지름은 **자리마다 다르다**: 헤더 26(시안 tts 프레임), 섹션 34(아래 무장애 뱃지와 맞춤).
    헤더 터치 영역은 34×44 다 — 44 로 넓히면 12pt 옆 지도 버튼과 겹친다(시안 간격 12).
    헤더/섹션이 **같은 `speechButton(_:section:diameter:touchWidth:)`** 을 쓴다.

  ⚠️ **시뮬레이터에는 오디오 출력 장치가 없어 낭독이 즉시 끝난다** — 눌러서는 읽는 중 상태를
  볼 수 없다. `isReading` 을 잠시 `true` 로 고정해 확인하고 되돌렸다(딥그린 원 + 흰 파형,
  아래 뱃지와 같은 크기). **소리와 파형 움직임은 실기기 확인 목록.**
- **#8 더 보기 모션 — 완료(2026-09-06).** 애니메이션을 **바꾸는 쪽에서** 껐다.

  예전에는 버튼에 `.animation(nil, value: isOverviewExpanded)` 를 걸어 뒀는데 새는 길이 둘 있었다:
  ① 그 모디파이어는 **버튼 아래만** 덮는다 — 실제로 높이가 바뀌는 것은 위의 `JustifiedText` 다.
  ② SwiftUI 는 **상위에서 내려온 트랜잭션을 물려받는다**(시트 표시·`withAnimation` 등)
  — 뷰 쪽에서 막는 것으로는 그 경로가 남는다.

  그래서 토글 자리에서 `withoutAnimation { }` 으로 끈다(`DesignSystem/NoAnimation.swift`,
  `disablesAnimations`). **이 저장소에 있으면서 쓰는 곳이 하나도 없던 헬퍼다** — 이 문제를
  위해 쓰인 것으로 보이는데 호출부가 없었다. 화살표에는 `.contentTransition(.identity)` 를
  못 박아 심볼 교체에 애니메이션이 붙지 못하게 했다.

  시뮬레이터 확인: 펼침·접힘 **양쪽 다 즉시** 바뀐다.

  남은 움직임 하나 — **버튼 눌림 흐려짐**(`.buttonStyle(.plain)` 의 기본 동작). 이것까지
  없애려면 `LoadMoreButton` 에 눌림 표시를 안 하는 `ButtonStyle` 을 줘야 하는데, 그 버튼은
  "맞춤 추천 더보기"·"리뷰 더보기"도 함께 쓴다 — 그 둘은 **누른 뒤 네트워크를 기다리므로
  눌림 표시가 유일한 반응**이다. 설명 더보기만 따로 끄고 싶으면 플래그를 받아야 한다.

  ⚠️ **곁들여 발견(QA 목록 밖, 아직 안 고침)**: "설명 더보기" 버튼이 **접을 것이 없을 때도
  뜬다.** 금배네민박은 설명이 6줄 안에 들어가서, 눌러도 글이 그대로인 채 버튼 글자만
  "설명 접기"로 바뀐다 — 고장으로 읽힌다. 고치려면 `JustifiedText` 가 **잘렸는지**를 알려
  줘야 한다(6줄 제한 높이와 전체 높이를 비교). 따로 잡는 게 좋겠다.

  ⚠️ **곁들여 발견(QA 목록 밖)**: "설명 더보기" 버튼이 **접을 것이 없을 때도 뜬다.**
  금배네민박은 눌러도 글이 그대로인 채 버튼 글자만 "설명 접기"로 바뀐다 — 고장으로 읽힌다.
  #8 고칠 때 같이 잡는다.

### 3묶음 · 홈 (3, 6, 11)

전부 피그마 대조가 필요하다. 관련 파일: `Features/Home/HomeView.swift`, `Components/PostCard.swift`,
`Components/PhotoPlaceholder.swift`(이미지 없을 때), `Features/Home/HeroCard.swift`.

- **#3 정렬 UI — 시안을 찾았다(2026-09-06).** 디자이너가 두 상태를 **따로** 그려 뒀고,
  화면 안에 붙어 있는 칩은 `13:303` 이다.

  | | 시안 | 앱 지금 |
  |---|---|---|
  | 닫힘 칩 | 캡슐 radius 999 · 흰 배경 · 보더 `#E6E6E6` 1 · 패딩 9/10/9/14 · gap 5 | 같다(`cardStroke` = `#E6E6E6`) |
  | 글자 | Noto Sans KR **Regular** 14 · `#075B39` | **Bold** 14 ← 다름 |
  | 화살표 | Material Symbols `expand_more` 19 | SF `chevron.down` 12 Bold |
  | **열림**(129:89) | **같은 자리에 세로 펼침** · radius **18**(캡슐 아님) · 87×90 · 간격 8 · **화살표 사라짐** · **체크마크 없음** | **popover** + 체크마크 ← 교체 대상 |
  | 항목 | 추천순 / 최신순 / **팔로잉** (3개) | 추천 / 최신 (2개) |

  **지금 popover 를 쓴 이유가 인라인으로 가면 사라진다** — 코드 주석대로 시스템 `Menu` 는
  스크롤 뷰 안에서 라벨이 플로팅 레이어에 남아 어긋난다. 인라인 펼침은 그 문제가 없다.

  구현 방향: **`overlay(alignment: .topTrailing)`**. 39 → 90pt 로 커지는 것을 인라인으로 넣으면
  아래 리뷰 카드가 51pt 밀린다. 위에 얹으면 시안 그림대로 보이면서 아무것도 움직이지 않고,
  시스템 popover 의 화살표·그림자 같은 군더더기도 없다.

  **정한 것 (2026-09-07)**
  1. **팔로잉은 넣지 않는다** — 서버에 팔로우 API 가 **아예 없고**(`grep follow` 0건) 앱의
     팔로우 버튼은 심사 지적 "준비 중" 항목이다. 추천/최신 둘만.
  2. **라벨은 "추천"/"최신"** — 화면 안 칩(13:303)과 같게. 상태 시안의 "추천순/최신순"은 안 쓴다.
  3. **체크마크는 남긴다** — 시안에는 없지만, 무엇이 골라져 있는지 목록에서 알 수 없으면
     접근성이 주제인 앱에서 앞뒤가 안 맞는다. **시안과 다른 지점으로 기록한다.**
  4. **글자는 Bold 로 유지** — 시안은 Regular 인데, Regular 로 내려 보니 너무 가벼워서
     요청자가 되돌렸다(2026-09-07). **시안과 다른 지점으로 기록한다.**

  **→ 결론: 직접 만들되 모양·동작을 시스템 메뉴에 맞췄다.**

  ⚠️ **시스템 `Menu` 는 쓸 수 없다.** 갈아 봤다가 되돌렸다 —
  **메뉴를 열어 둔 채로 스크롤하면 메뉴가 위아래로 흔들린다.** 시스템 메뉴는 스크롤 뷰
  **밖의** 플로팅 레이어에 뜨는데 앵커는 스크롤과 함께 움직이기 때문이다. 원래 주석에
  적혀 있던 그 증상이고, **아직 고쳐지지 않았다.**

  ⚠️ **확인 방법을 틀렸던 기록**: 처음에 "고쳐졌다"고 판단했는데, **닫은 상태로만
  스크롤해 봤기 때문**이다. 이 증상은 **열어 두고 스크롤해야** 나온다. 플로팅 레이어를
  쓰는 것(메뉴·팝오버·툴팁)을 스크롤 뷰 안에서 검증할 때는 **열어 둔 채로 스크롤**한다.

  직접 만든 것의 구조:
  - 펼친 목록을 **칩의 `overlay`** 로 얹는다 → **스크롤 뷰 안**에 있어서 목록과 함께 움직인다.
    흔들릴 수가 없다. 레이아웃도 밀지 않아 열어도 아래 카드가 내려가지 않는다.
  - 섹션 머리 줄에 **`.zIndex(1)`** — `VStack` 은 뒤에 오는 형제를 나중에 그려서,
    없으면 리뷰 카드가 펼친 목록을 덮는다.
  - 모양은 시스템 메뉴에 맞췄다: **체크를 왼쪽**에, 줄 높이 44, radius 14 + 옅은 그림자.
    체크 자리는 늘 잡아 둬 고를 때 글자가 좌우로 움직이지 않는다.
  - 여닫기는 `withoutAnimation` — 스르륵 펼쳐지면 카드 위에서 덮개가 자라는 것으로 보인다.
  - 화살표가 뒤집혀(▼/▲) 열림 상태를 모양으로도 알린다.

  남은 차이 하나: **바깥을 눌러 닫는 동작이 없다**(칩을 다시 누르거나 항목을 고르면 닫힌다).
  스크롤해도 열린 채 칩에 붙어 따라온다. 필요하면 앵커 프리퍼런스로 화면 최상단에 올려
  탭 캐처를 두는 방법이 있지만, 그러면 다시 스크롤 뷰 밖으로 나가게 된다.
- **#6 이미지 없는 카드 — 완료(2026-09-07).** 요청자가 값을 직접 줬다(피그마에서 `card default`
  라는 이름의 노드는 못 찾았다 — 로드된 페이지에 없다).

  | | 전 | 후 |
  |---|---|---|
  | 배경 | 연한 라임 → 회색 그라데이션 | **`#FAFAFA` 단색**(`Color.cardPhotoEmpty`) |
  | 카테고리 아이콘 | 딥그린 @0.55 | **흰색** |
  | 접근성 뱃지 | 딥그린 원 + 흰 아이콘(`.filled`) | **흰 원 + 딥그린**(`.inverted`) — 사진 있는 카드와 같게 |

  **셋으로 갈려 있던 것을 하나로 모았다** — 같은 "사진 없음" 상황을 세 카드가 다르게 그리고
  있었다: 홈 추천(라임 그라데이션 + 딥그린), 저장 목록(회색 + 옅은 딥그린), 함께 가볼만한 곳은
  **개발용 `PhotoPlaceholder`("장소 사진" 이라 적힌 상자)** 를 그대로 써서 그 글자가 사용자
  화면에 보였다. 새 `Components/PlaceCategoryArtwork.swift` 하나를 셋이 함께 쓴다.

  ⚠️ **흰 아이콘 대비는 1.02:1 이다** — 거의 안 보이는 "유령" 표현이고 의도된 것이다.
  WCAG 1.4.11(비텍스트 3:1)에 걸리지 않는 이유는 **장식**이기 때문이다: 스크린리더에서 감추고
  (`accessibilityHidden`) 카테고리는 카드 본문에도 글자로 있다. 고대비를 켜면 배경만 짙어져
  살아난다(1.02:1 → 1.27:1).

  ⚠️ **함께 가볼만한 곳은 아이콘을 못 그린다** — `RelatedPlace` 에 카테고리 필드가 없다
  (서버 응답에 없어서 `RelatedPlace.place` 가 `.attraction` 으로 고정해 둔다). 그 값으로
  그리면 호텔에 관광지 그림이 붙으므로 **배경만** 그린다. 서버가 카테고리를 실어 주면 붙는다.
  → 남이 해 줘야 하는 것에 추가.

### 4묶음 · 프로필·마이페이지 (2, 17, 18, 5) — 제일 크다

- **#2** 지금은 `마이페이지 → 9개 줄 → 각 화면` 3단이다(`Features/Auth/AccountSettingsView.swift`
  의 `Row` enum: accessProfile, profileEdit, myPosts, blockedUsers, accessibility, helpCenter,
  terms, privacyPolicy, 탈퇴). 어디를 어디로 합치는지에 따라 작업이 완전히 갈린다 → 질문.
- **#5** 애플 버튼은 **애플이 주는 컴포넌트**(`SignInWithAppleButton`, `AuthFormControls.swift:486`
  주석 참고)를 그대로 쓴다. 로고·문구·색은 애플 규정상 못 바꾸고 **크기와 모서리만** 맞출 수 있다.
- **#17/#18** `Features/Auth/ProfileEditView.swift`, `AccessibilityProfileEditView.swift`.

### 5묶음 · 플랜 (13, 14, 16)

**#13 현황** — 지금 고칠 수 있는 것:
- 제목: 상세 → `PlanTitleEditView`
- 일정 순서: 상세 "편집" → `PlanEditView`(Day 넘나드는 드래그)
- 팀(인원): 목록 카드 컨텍스트 메뉴 "팀 수정" → `PlanPartyEditView`

**없는 것: 여행 날짜/기간, 지역.** #13 이 이 둘을 말하는 것인지 확인 필요 → 질문.

### 추가로 들어온 것 (원문 19건 밖)

- **저장됨이면 북마크를 채운다** — 시안에는 채워진 북마크 상태가 **없다**(액션 줄의
  `Bookmark`(249:772) 하나뿐). 다만 시안의 bookmark 컴포넌트에는 `default`/`selected` 변형이
  있고 `selected` 는 이미 `tab_saved_fill` 로 뽑혀 있다 — 그 글리프를 detail 용으로
  `detail_bookmark_fill` 에 넣었다(아웃라인과 **같은 path** 에 fill 만 더해 크기가 정확히 맞는다).
  ⚠️ 저장은 로그인이 필요해 시뮬레이터에서 눌러 볼 수 없다 — `isSaved` 를 잠시 고정해
  확인하고 되돌렸다. **실기기 확인 목록.**

### 6묶음 · 글쓰기·리뷰 (10, 12)

- **#10** `Features/PostCompose/PostComposeView.swift` (693줄) — 피그마 필요.
- **#12** 리뷰 작성의 pill 아이콘. `Components/ReviewTagViews.swift`,
  `Features/ReviewCompose/ReviewComposeView.swift`.

## 물어본 것 (답 오면 여기에 적는다)

1. **#1 "중복 데이터 삭제"** — **답 받았다(스크린샷). 고쳤다.**
   추가정보 목록에 **똑같은 문장이 두 번** 나오는 것이었다(라마다 구미: "엘리베이터 있음",
   "장애인 주차장 있음(4면)"이 목록 끝에 다시 붙었다).

   ⚠️ **서버 문제가 아니라 앱 문제였고, 이 저장소가 만든 것이다.** `PlaceDetail.accessibilityNotes`
   가 `accessibilityGroups.flatMap(\.notes)` 였는데, **고령자 그룹이 휠체어 대여·승강기·주차
   원문을 지체장애 그룹과 함께 쓴다**(관광공사 원본에 고령자 전용 속성이 없어서 — 2026-09-05 에
   그 그룹을 상세에 추가하면서 생겼다). 그래서 두 유형에 다 해당하는 장소에서 같은 줄이 두 번
   나왔다. 낭독도 이 목록을 읽어 같은 문장을 두 번 읽었다 — QA 가 TTS 항목에 묶어 적은 이유다.

   고친 방식: 합칠 때만 같은 문장을 한 번으로 줄인다(순서 유지). **뱃지별 안내 칩은 그룹의
   `notes` 를 그대로 쓰므로 영향이 없다** — 고령자 뱃지를 누르면 승강기·주차 안내가 그대로 나온다.
   시뮬레이터 확인: 10줄 → 8줄, 끝의 중복 두 줄이 사라졌다.
2. **#1 파형 아이콘** — **시안에 없다(2026-09-06 확인).** `Asset_Icon` 페이지(328:911)에 파형
   아이콘이 없고, 섹션 헤더의 `tts` 노드(1081:208)에는 헤드폰 인스턴스 하나뿐이라 "선택시"
   변형이 없다. 디자인 시스템 검색도 0건 — 헤드폰이 온 아이콘 라이브러리가 파일에 연결돼
   있지 않다. → 디자이너에게 요청하거나 애플 기본 `waveform` 으로 간다(후자는
   `symbolEffect(.variableColor)` 로 읽는 중에 실제로 파형이 움직인다).
   딥그린 원은 26 → 34pt 제안(추가정보 뱃지와 같은 크기).
   → 답:
3. **#2 마이페이지 2depth** — 마이페이지 자체를 없애고 탭에서 바로 가는가, 줄 몇 개를 마이페이지
   본문에 펼치는가?
   → 답:
4. **#13 "여행 수정"** — 여행 **날짜/기간과 지역**을 말하는 것인가?
   → 답:
5. **#5 Apple 버튼** — 카카오/구글 버튼 규격(320×51, radius 18)에 맞추면 되는가?
   → 답:
6. **#10, #11, #14** — 피그마 노드 번호나 페이지 이름.
   → 답:

## 작업 규칙

- 묶음 하나를 끝낼 때마다 커밋한다. 커밋 메시지는 이 저장소 관례대로 **무엇이 달라졌는지**를
  한국어 한 줄로 쓴다(`git log` 참고).
- 값이 시안과 다르면 **위치 오프셋으로 때우지 말고 근본 입력값**(폰트 크기·자간·간격)을 고친다.
- 피그마 대조는 노드 하나씩 열어 본다 — 상위 프레임만 보면 하위 트리에 있는 지시를 놓친다
  (실제로 TTS 버튼 위치를 그렇게 놓쳤다).
- 시뮬레이터로 확인할 수 없는 것(소리·유니버설 링크·로그인 필요 화면)은 실기기 확인 목록에 남긴다.
